   /* function drawScores(){
    points +=250 ;
      this.context.font = '20px Press+Start+2P';
      this.context.fillStyle = 'white';
      this.context.fillText('Score: ' + points, 60, 40);
    }
    var countDownValue=0;
    function timeRemaining(remainingTime){
     var startTime = Date.now();
        return function() {
           return remainingTime - ( Date.now() - startTime );
        }
        var currentCountDown = timeRemaining(120000);
        var countDownValue = currentCountDown()
    }*/
